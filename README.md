# Unmatched Web Scraper

Web scraper for hero statistics from the Unmatched board game series.

In order to install all dependencies run:

```pip install -r requirements.txt```

### Note for developers:

When adding new packages, please make sure to update the `requirements.txt` file by running:

```pip list --user --format=freeze > requirements.txt```